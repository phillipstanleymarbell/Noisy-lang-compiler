#
#	The `Math` progtype implements math routines.
#
Math : module (valueType: type)
{
	cos	: function (arg: valueType) -> (result: ValueType);
	sin	: function (arg: valueType) -> (result: ValueType);

	min	: function (a: valueType, b: valueType) -> (result: ValueType);
	max	: function (a: valueType, b: valueType) -> (result: ValueType);
}
